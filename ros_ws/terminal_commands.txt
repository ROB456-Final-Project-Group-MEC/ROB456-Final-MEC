
#if using Stage, make sure to open Ubuntu in Xorg from sign-in screen

# enter into the ros_ws folder
    cd Desktop/CodingProjects/ROB456/ros_ws/

# init ros2 in the terminal
    source /opt/ros/kilted/setup.bash

# build the environment (do this in the ros_ws folder)
    colcon build

# setup your code to run for this session
    source install/local_setup.bash


# ----------------------------------------------------- lab 0 -----------------------------------------------------
# example of running a program
    ros2 run lab0 circler


# ----------------------------------------------------- lab 1 -----------------------------------------------------
# example of launching Stage
    ros2 launch rob_stage rob_stage.launch.py world:=simple

# open Rviz2
    rviz2


# ----------------------------------------------------- lab 2 -----------------------------------------------------
# lab2 with obstacle world and rviz
    ros2 launch lab2 lab2.launch.py world:=obstacle launch_rviz:=true

# lab2 Extra Credit launch with simple world and rviz
    ros2 launch lab2 lab2.launch.py world:=simple launch_rviz:=true

# or individually
    ros2 launch rob_stage rob_stage.launch.py world:=obstacle
    ros2 run lab2 send_points
    ros2 run lab2 driver


# ----------------------------------------------------- lab 3 -----------------------------------------------------
# open Rviz2 config file
    rviz2 -d src/lab3/config/driver.rviz

# run the lab 3 launch file
    ros2 launch lab3 lab3.launch.py world:=simple launch_rviz:=true

# individual terminal commands to run each of the nodes

                            # ---- installing stage ----

                            # 1. cd into the source folder
                            cd Desktop/CodingProjects/ROB456-Final-MEC/ros_ws/src

                            # 2. clone stage_ros2 from github
                            git clone https://github.com/tuw-robotics/stage_ros2.git

                            # 3. go back to the ros workspace
                            cd ..

                            # 4. install dependancies (requires password)
                            rosdep install --from-paths src --ignore-src -r -y

                            # 5. colcon build the new stage package specifically
                            colcon build --symlink-install --packages-select stage_ros2

                            # 6. resource the workspace
                            source install/setup.bash


# have to install imageio 

                            # install it with this terminal command

                            sudo apt update
                            sudo apt install python3-imageio



    

